---
---

Add a Standalone button beside GitHub and the homepage theme switch, using the same button styling. Resolve the newest published full-suite Electron release from GitHub, excluding individual app releases, drafts and prereleases, with a release-list fallback when the API is unavailable. Keep header links visible with touch-sized targets on phones.
